/**
 * Configuration package for Spring Boot configuration classes.
 * 
 * Contains:
 * - SecurityConfig: OAuth 2.0 + PKCE authentication configuration
 * - DatabaseConfig: HikariCP connection pooling and JPA configuration
 * - WebConfig: Web layer configuration and CORS settings
 * 
 * @since 1.0.0
 */
package com.globobank.fraud.config;